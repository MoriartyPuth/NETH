# --- core (required) ---
crccheck>=1.3          # CRC-16/CCITT-FALSE for KHQR checksum
fastapi>=0.110         # API + web UI server
uvicorn[standard]>=0.27
pydantic>=2.0
requests>=2.31         # Bakong account verification
pyyaml>=6.0            # data/bank_domains.yaml loader

# --- vision (QR photo scanning) ---
# Use the CONTRIB build to enable the WeChat QR detector (reads logo/stylized
# QR like ABA Pay). It replaces opencv-python(-headless) — install only one.
opencv-contrib-python-headless>=4.9
numpy>=1.26
pyzbar>=0.1.9          # multi-QR decode fallback (needs zbar system lib)
# Then fetch the WeChat models: python scripts/fetch_wechat_models.py

# --- nlp (optional, for the transformer upgrade) ---
# transformers>=4.40
# torch>=2.2
# khmer-nltk>=1.6      # Khmer word segmentation

# --- telegram bot (optional) ---
# python-telegram-bot>=21.0

# --- dev ---
pytest>=8.0
